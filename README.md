# skripts
# 🐚 Полезные Bash-скрипты для повседневных задач

Набор простых, но мощных Bash-скриптов для автоматизации рутинных операций в терминале.  
Работают в **Git Bash на Windows**, WSL, macOS и Linux.

> 💡 Все скрипты протестированы в Git Bash (MINGW64) — идеально для пользователей Windows + VS Code.

---

## 📜 Список скриптов

| Скрипт | Назначение | Файл |
|-------|-----------|------|
| **Приветствие** | Спрашивает имя и выводит персонализированное приветствие | `hello.sh` |
| **Калькулятор суммы** | Запрашивает два числа и выводит их сумму | `sum_calculator.sh` |
| **Проверка чётности** | Определяет, чётное число или нечётное | `check_even.sh` |
| **Создание структуры проекта** | Генерирует базовую структуру веб-проекта (HTML, CSS, JS) | `create_web_project.sh` |
| **Счётчик строк** | Подсчитывает количество строк в указанном файле | `count_lines.sh` |
| **Генератор паролей** | Создаёт случайный 8-символьный пароль (буквы + цифры) | `generate_password.sh` |
| **Поиск файлов по расширению** | Находит все файлы с заданным расширением в текущей папке | `find_by_extension.sh` |

---

## ▶️ Как запустить

1. Убедитесь, что у вас установлен [Git for Windows](https://git-scm.com/download/win) (включает Git Bash).
2. Откройте терминал **Git Bash** (или используйте его в VS Code).
3. Перейдите в папку со скриптами:
   ```bash
   cd /путь/к/папке-со-скриптами
